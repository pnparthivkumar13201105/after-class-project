import calendar

def display_month_names():
    month_names = list(calendar.month_name)
    
    month_names = [month for month in month_names if month]
    
    print("Months of the Year:")
    for month in month_names:
        print(month)

display_month_names()
